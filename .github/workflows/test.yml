name: Test
on: push

jobs: 
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2.3.5
      - name: Install Gulp
        run: |
          npm install gulp-cli
          gulp -v
      - name: Build
        run: gulp build package
